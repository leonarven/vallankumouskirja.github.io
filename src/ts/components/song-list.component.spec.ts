/*
import { TestBed, async } from '@angular/core/testing';
import { RouterTestingModule } from '@angular/router/testing';
import { NO_ERRORS_SCHEMA } from '@angular/core';



import { SongListComponent } from './song-list.component';

fdescribe('SongListComponent', () => {

  let songListComponent: SongListComponent;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      imports: [
        RouterTestingModule
      ],
      declarations: [
        SongListComponent
      ],
      schemas: [ NO_ERRORS_SCHEMA ]
    }).compileComponents();
  }));

  beforeEach(() => {

    const fixture = TestBed.createComponent( SongListComponent );
    songListComponent = fixture.debugElement.componentInstance;
//    appComponent.itemList = [{id: 1, name: 'todolist', description: 'This is todo list app'},
  //                    {id: 2, name: 'unit test', description: 'Write unit tests'}];
  });

  it('should create the app', () => {
    expect( songListComponent ).toBeTruthy();
  });
});
*/
